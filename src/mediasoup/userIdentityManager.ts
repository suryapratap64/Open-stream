/**
 * User Identity Manager - Backend Version
 * This module is NOT used on backend.
 * See frontend/src/utils/userIdentityManager.ts for the client-side version
 *
 * The userId is managed entirely on the client side using localStorage
 * and passed to the server in the join event.
 */

export class UserIdentityManager {
  // This class is not used on the backend
  // It exists only to satisfy import statements
  // All userId management happens on the client side
}
